package Moyoung.Server.crawler.repository;

import Moyoung.Server.crawler.entity.Cinema_crawler;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Cinema_crawlerRepository extends JpaRepository<Cinema_crawler, Long> {
}
